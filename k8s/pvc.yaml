apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fuse-models-pvc
  namespace: fuse-system
  labels:
    app: fuse-api
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: fast-ssd
